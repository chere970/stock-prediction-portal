creating react app using vite
pip install python-decouple=>to use .env file, to config secrete key inside setting.py file
Used djecrety to create django secret key
used generate gitignore to generate .gitignore file's text(code)
npm i react-router-dom // to use router
npm i axios // to connect react with backend(api end point)
pip install django-cors-headers and added it to installed_apps// to enable cors
s